# NexusStream Backend - Development Dockerfile
# Supports hot-reloading with ts-node-dev

FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source code
COPY tsconfig.json ./
COPY src/ ./src/

# Create data directory
RUN mkdir -p /app/data

# Expose port
EXPOSE 3000

# Development command with hot reload
CMD ["npm", "run", "dev"]
